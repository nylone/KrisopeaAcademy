CREATE TABLE CLIENTI(
    COGNOME VARCHAR2(30) NOT NULL,
    NOME VARCHAR2(30) NOT NULL,
    CITTA VARCHAR2(30) NOT NULL,
    SALARIO NUMBER(7) NOT NULL,
    ETA NUMBER(3) NOT NULL
);

INSERT INTO CLIENTI VALUES(
    '&COGNOME',
    '&NOME',
    '&CITTA',
    &SALARIO,
    &ETA
);

-- 1
SELECT
    DISTINCT COGNOME
FROM
    CLIENTI;

-- 2
SELECT
    DISTINCT NOME
FROM
    CLIENTI;

-- 3
SELECT
    DISTINCT COGNOME,
    NOME
FROM
    CLIENTI;

-- 4
SELECT
    COGNOME,
    NOME,
    CITTA
FROM
    CLIENTI;

-- 5
SELECT
    *
FROM
    CLIENTI;